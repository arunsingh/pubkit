Install:

```bash
pip install "pubkit[all]"
playwright install chromium   # browser adapters only
pubkit init my-blog && cd my-blog
pubkit validate content/
```

See [docs/FAILURE-MODES.md](https://github.com/arunsingh/pubkit/blob/main/docs/FAILURE-MODES.md)
for what this tool actually guards against.
